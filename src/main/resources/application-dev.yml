spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql:kailash
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true